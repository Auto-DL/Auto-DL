import { createTheme } from '@material-ui/core/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#252934'
    },
    secondary: {
      main: '#0066FF',
    },
    background: {
      paper: '#F1F3F7',
      default: '#F1F3F7'
    }
  },
});

export default theme;
